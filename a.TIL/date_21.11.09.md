# Date 2021.11.09
## ✏️TIL
### 형타입(데이터타입)
🔖 배열,함수 다시 공부 필요 ‼️
>7가지 타입 (기본형 5개 + 특수형 2개)

🧷 <기본형>
- 숫자: js는 실수, 정수 구분 없음
- 문자: '' 내부에 글자 작성
- 논리형: 참(true)/거짓(false)을 의미하는 결과물을 도출 - true,false는 문자가 아니기 때문에 ''를 붙일 수 없음
- undefined: 값이 미정인 상태의 결과값이 존재
- null: 값이 없다./ 값 전달 시 없는 값

🧷 <특수형>
- 객체(배열/객체)
  - 배열<br />
    ▶️ html문서에서 ol처럼 여러개의 목록으로 []에 내용을 나열하는 것
  - 객체<br />
    ▶️ 배열처럼 나열하지만 순서 X <br />
    ▶️ 해당하는 목록에 대한 정의를 하는 것 html문서에서 dl/css의 속성 형태의 개념과 같음 <br />
    ▶️ {} 내부에 여러개의 내용을 담아서 그룹화 시킴
- 함수<br />
  ▶️ 어떤 행동을 처리하기 위해 만들어 놓은 묶음<br />
  ▶️ 선언식, 표현식, 즉시 실행 함수

📍 다른 프로그래밍 언어와의 차이점<br />
    객체 내부에 배열을 담거나 함수를 담을 수 O

---
- typeof(변수명): 데이터 타입(형타입)을 파악하는 기능
- parse...(변수명): 숫자로 보이는 문자 ('1')를 강제로 숫자형 타입으로 변환하는 함수 ('1' -> 1 로 바꿔줌)
  - parseInt(변수명): 정수를 변환 <br />
  ex) '1.3' ➡️ 1
  - parseFloat(변수명): 소수점을 변환 <br />
  ex) '1.3' ➡️ 1.3
  - Number(변수): 강제로 숫자로 만들었으니 숫자로 만들어는 주나 무슨 뜻인지는 모름
  ex) NaN(Not a Number)

>숫자(number)

숫자의 형타입은 숫자와 함께있기 때문에 'number' 즉, ''가 붙음
```
var num=7;
console.log(num, typeof(num));
⬇️
7 'number'
```
---
🧷 수학 객체 메서드 Math 함수<br />
|종류|설명|
|----|----|
console.log(Math.ceil(변수명));  |올림처리
console.log(Math.floor(변수명)); |내림처리
console.log(Math.round(변수명)); | 반올림처리
console.log(Math.random(변수명));|0~1까지의 난수<br /> ** 1~10까지의 난수를 원할땐 console.log(Math.random(변수명)*10);
console.log(Math.abs(-변수명));  | 절대값<br />
➡️ 변수명에 - 를 붙여도 그대로 나온다

>문자(string)

var 변수명='';에서 ''안에 내용이 비어 있어도 값은 나옴
```
var str='안녕';
console.log(str, typeof(text));
⬇️
안녕 string 
```
예를 들어, 아래와 같은 상황일땐
```
var str = '123';
var str2 = str;
    str = '문자';

console.log( str, typeof(str) );
console.log( str2, typeof(str2) );

var str3 = str2 + 567;
console.log( str3 );
⬇️
123567
```
str2=str='123'
따라서, 결과는 123567이라고 써 있지만 사실은 '123'567 <br />
즉, 문자 + 숫자 이다.

>논리(boolean)

```
var bool=false;
console.log(bool, typeof(bool));
⬇️
false 'boolean'
```
- 결과를 true/false으로만 처리 But 그 결과를 확인하는 검증이 많음 <br />
  - ! : 반대
    - !를 두번 쓰는 이유: 존재하는 값에 !를 붙이면 값이 없다는 의미 <br /> ➡️ false가 되지만 본의도는 값이 있느냐 없느냐 판단용도로 쓰면 !!
  - === : 형타입까지 일치하는가?

>undefined
```
var und = undefined;
console.log( und, typeof(und) );
⬇️
undefined 'undefined'
````
만약 `und = undefined + 'result';`일 경우 결과 값은 ndefinedresult string 으로 나온다. <br />
즉, undefined는 정의가 되지 않은 속성이기 때문에 뒤에 어떤 속성을 붙이든 그 속성을 따라 정의가 된다.

>null

null은 값이 없지만 object라는 형타입을 가짐

📌 <b>요약</b>
  - undefined와 null 형타입은 단순히 값이 누락시킨다는 의미
  - object/function 형타입은 여러 형타입을 담은 복합성의 특징<br />
    ➡️ 참조변수 개념으로 처리<br />
    ✋🏻 참조변수: 어느 공간(주소)을 공유하는 변수 (값을 가지는 변수X)<br />
       일반변수: 단순 값을 갖는 변수

>객체(object)

  🧷 배열(array)
  - var 참조변수=[];
  - constructor: 형타입을 구축한 원기본형태는 배열로 처리한 객체라는 의미
  ```
  var 변수명=['목','록','들','그','대','로','나','옴'];
  console.log(변수명, typeof(변수명));
  ⬇️
  🔽 ['목','록','들','그','대','로','나','옴'] object
  0: "목"
  1: "록"
  2: "들"
  3: "그"
  4: "대"
  5: "로"
  6: "나"
  7: "옴"
  ```
  
  목록들 중에서 한가지만 골라서 결과가 나오게 하고 싶을 땐 [n]작성
  
  단, js는 첫번째 순서가 0부터 시작 된다.
  ```
  var 변수명=['목','록','들','그','대','로','나','옴'];
  console.log(변수명[3]);
  ⬇️
  그
  ```

  목록들 중에 한가지만 내용을 바꾸고 싶을 땐 바꾸고 싶은 내용이 몇번짼지 확인 후 [n]작성
  ```
  var 변수명=['목','록','들','그','대','로','나','옴'];  
  변수명[3] = '고';
  console.log(변수명[3]);
  ⬇️
  🔽 ['목','록','들','고','대','로','나','옴'] object
  0: "목"
  1: "록"
  2: "들"
  3: "고"
  4: "대"
  5: "로"
  6: "나"
  7: "옴"
  ```
  ** 넘치는 n번째 숫자를 적을 경우 undefined가 나옴
  
  반대로
  ```
  var 변수명=['목','록','들','그','대','로','나','옴'];  
  변수명[9] = '고';
  console.log(변수명[9],변수명);
  ⬇️
  🔽 ['목','록','들','그','대','로','나',비어있음x2,'옴'] object
  0: "목"
  1: "록"
  2: "들"
  3: "고"
  4: "대"
  5: "로"
  6: "나"
  9: "옴"  
  ```

  📎 배열객체 메서드 속성

  |종류             |설명                                         |
  |-----------------|---------------------------------------------|
  |push(new data)   |배열 객체의 마지막 인덱스에 새 데이터 삽입   |
  |shift()          |배열 객체에 저장된 데이터 중 첫번째 인덱스에 저장된 데이터 삭제  |
  |unshift(new data)|배열 객체의 가장 앞 인덱스에 새 데이터 삽입  |
  |pop()            |배열에서 저장된 데이터 중 마지막 인덱스에 저장된 데이터 삭제|
  
🧷 객체
```
obj.apple = 'iphone';
obj.samsung = 'galaxy';
obj.google = 'pixel';
obj.['농심'] = '너구리';
obj.['오뚜기'] = '진라면';
obj['한국 야쿠르트'] = '건국우유';
⬇️
{apple:'iphone', samsung:'galaxy', google:'pixel', 농심:'너구리', 오뚜기:'진라면', 한국 야쿠르트:'건국우유'}

⏩ 띄어쓰기가 있는 속성들은 []안에 ''와 함께 써줘야 오류가 안뜸
   영문 이름이 아닌 경우에도 마찬가지로 써주는 것이 좋음
```
- `console.log( obj.google );`에서 .google이라는 것을 propoty라고 함
- 객체는 .의 표현과 []의 표현을 같이 가지고 있음

❓배열과 똑같이 먹기때문 따라서, 배열=객체 / 객체=배열 

📌 배열과 객체 구분법
- 배열: []
- 객체: {}

>함수

함수의 기본형태 = function(){} 이렇게 이름이 없는 함수를 <b>익명함수</b>라고 함(변수선언: 이름 대신 이름을 할당할 수 있는 무언가를 선언)<br />
➡️ 함수 표현식: 익명함수로 처리하는 기법

function 이름(){} 이렇게 이름을 부여하는 함수를 <b>기명함수</b> 라고 함<br />
➡️ 함수 선언식: 기명함수로 처리하는 기법

🧷 function

var 변수명=function(){};<br />
var - 변수, 변수명 - 변수 선언, function(){} - 함수

함수는 어떤 기능을 담아 뒀다가 그 역할을 부여하면 그제서야 동작을 함<br />
```
ex)var fn=function(a){
  return 1+a; <-a=인자
};

console.log(fn(5)); <-(5)= 인수
⬇️
6
```
function(매개변수,인수/인자){};
return: 마지막에 작성하는 것,어쨌든 최종 결과가 뭔지 알릴 때 사용

❓수를 입력하였을때 그 결과물을 확인하고 체크하는 기능을 가진 것

🖇 함수선언식

- 호이스팅의 영향을 받는다

🖇 함수표현식

- 호이스팅의 영향을 받지 않는다.
- 함수 선언식보다 지향

🖇 스코프
- 지역변수:  어떤 범위 안에서만 동작 가능
- 전역변수: 광범위한 범위를 가지고 있음

✋🏻 함수 내부에서 var를 선언하느냐에 따라 원 전역 변수의 값이 변경 되느냐 아니냐로 구분 <br />

즉시 실행 함수
-  익명 함수의 기다림 없이 바로 실행: (function(){})() / (function(){}()) <br />
  ➡️ 단지 그룹을 어느 위치에서 묶어줄 것이냐의 차이

🚫 배열
- 참조변수
- 배열=[], 객체={} 인 것만 알면 되는지?

🚫 함수
- 변수선언
- 함수표현식, 함수선언식
- 매개변수, 인수/인자

‼️ 결론은 배열과 함수를 이해하지 못함 완전 추가 설명&공부 필요

📌 <b>형타입 정리(요약)</b>
